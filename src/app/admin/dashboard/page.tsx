import { AdminOverviewView } from '../_components/admin-overview-view';

export const metadata = {
  title: 'لوحة القيادة والمراقبة الشاملة — Super Admin',
};

export default function AdminDashboardPage() {
  return <AdminOverviewView />;
}
